function add_cca_paths()
% ADD_CCA_PATHS  Repository stand-in for the lab's Analysis Suite/PIPELINE/add_cca_paths.m.
%   The lab version changed folder and added hard-coded Dropbox folders; all code is
%   already on the path via startup_sm.m, so this does nothing.
end
